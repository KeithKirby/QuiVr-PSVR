using UnityEngine;
using System.Collections;
using System.Collections.Generic;
//using VolumetricFogAndMist;
public class FogSystem : MonoBehaviour {

    /*
    public static List<FogSystem> AllSystems;
	void Awake()
    {
        if (AllSystems == null)
            AllSystems = new List<FogSystem>();
        AllSystems.Add(this);
    }

    public static VolumetricFog[] GetSystems()
    {
        List<VolumetricFog> fogs = new List<VolumetricFog>();
        if(AllSystems != null)
        {
            foreach(var v in AllSystems)
            {
                VolumetricFog f = v.GetComponent<VolumetricFog>();
                if (f != null)
                    fogs.Add(f);
            }
        }
        return fogs.ToArray();
    }
    */
}
